{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "mw-analytics-schema@1/import_losses.schema.json",
  "title": "import_losses",
  "description": "One row per loss-report mapping.",
  "type": "object",
  "additionalProperties": false,
  "properties": {
    "import_artifact_hash": {"type":"string"},
    "project": {"type":"string"},
    "binding_id": {"type":"string"},
    "binding_version": {"type":"string"},
    "construct": {"type":"string"},
    "subject": {"type":"string"},
    "severity": {"type":"string"},
    "note": {"type":"string"}
  },
  "required": ["import_artifact_hash", "project", "binding_id", "binding_version", "construct", "subject", "severity", "note"]
}
