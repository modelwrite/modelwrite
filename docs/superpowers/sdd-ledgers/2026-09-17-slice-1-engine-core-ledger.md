# SDD ledger — plan: docs/superpowers/plans/2026-09-17-modelwrite-p0-p1-engine-core.md

Spec: docs/superpowers/specs/2026-09-17-modelwrite-platform-design.md (v2, binding authority)
Slice 1 plan: docs/superpowers/plans/2026-09-17-modelwrite-p0-p1-engine-core.md
Working branch: slice-1-engine-core (baseline main commit 5085dcd)

## Pre-flight conflict scan

| Pair / task | Produces vs consumes | Finding |
|---|---|---|
| T1 -> T2..T11 | T1 creates repo files, docs/okf, docs/evidence, sample/, agents/CLAUDE.md | No conflict; T1 creates directories that later tasks fill. |
| T1 -> T1 | 13-step task; step 13 uses git add -A | Finding: git add -A would sweep untracked collateral. Ruled: collateral committed to the baseline before Task 1. |
| T2 -> T4 | T2 schema required fields [project, summary, stateMachine, graph]; T4 types make summary/stateMachine/graph optional with validator enforcement | Consistent: schema states document requirements, validator enforces them at runtime. Spec section 7 lists both. |
| T3 -> T4, T5, T6, T7, T8 | T3 creates the expected OKF fixture path used by test_support | Match: sample/corpus/coffee-machine/okf/expected/coffee_machine_model.json. |
| T3 -> T4, T6 | test_support::load_okf_broken reads the corrupted fixture, but T6 step 6 creates it | Ordering is safe: load_okf_broken is only called from mw-gate tests (T6), after the fixture exists. No change needed. |
| T4 -> T5 | T5 graph consumes okf::types::OkfRoot, requirements, graph | Names match (OkfRoot, graph.nodes, graph.edges, requirements, req_id). |
| T4 -> T6 | T6 gate consumes okf::{validate, diff, hash} | Signatures match (validate(&OkfRoot) -> ValidationReport, diff(&,&) -> DiffReport, canonical_hash(&) -> String); report fields match what gate reads. |
| T5 -> T6 | T6 gate consumes graph::{graph_stats, requirement_coverage} | Signatures and field names match (isolated, component_count, uncovered). |
| T6 -> T7, T8 | T7 capi and T8 mcp consume gate::run | Match: run(&OkfRoot, &OkfRoot, bool) -> GateOutcome { passed, failures, evidence }. |
| T6 -> T9, T10 | CI (T9) runs mw-gate on the corpus; judge (T10) spawns mw-gate | T9 lands before T10 and its judge test step needs judge/tests, which T10 creates. Plan already notes: complete Task 10 before pushing. No change needed. |
| T9 -> T11 | T11 verification sequence runs fmt, clippy, tests, judge | Consistent with T9 pipeline. |
| T1 -> T4 | T1 creates docs/okf directory; T2 fills it with spec and schema | Fine; empty directories are not tracked by git, T2 adds real files. |
| T4 -> T4 | Step 12 expects 13 tests; the specified test files contain 11 (7 in okf_validation.rs + 4 in okf_diff.rs) | Finding: count mismatch. Ruled: all specified tests must pass; 11 is the correct count. |
| T1 -> T1 | Step 13 says the listing shows ten created files; the Files list names 9 files plus 3 directories | Finding: descriptive count is off by one. Ruled: 9 files plus 3 directories; no action beyond noting it. |
| T6 -> T6 | make_corrupted.py writes corrupted/coffee_machine_model.json; test_support::okf_broken reads the same path | Match (same filename, different directory), which is also what the judge pairing requires. |

## Rulings

Ruling: initialise git and work on branch slice-1-engine-core, with the documentation baseline on main — the plan was written for a repo that had no git yet, and the execution process forbids implementing on main. Cost if wrong: renames or a fast-forward merge; trivial.
Ruling: obtain rustup from https://static.rust-lang.org/rustup/dist/x86_64-pc-windows-msvc/rustup-init.exe instead of https://win.rustup.rs, which is unreachable from this machine (static.rust-lang.org, sh.rustup.rs and crates.io are reachable). Cost if wrong: none; the winget fallback also exists.
Ruling: Task 1 step 12 test-count wording is descriptive; the binding requirement is that every specified test passes. Cost if wrong: none.
Ruling: commit the knowledge-pack collateral to the repository as source material (4.94 MB, the author's own course work, credited in NOTICE.md) so that git add -A in Task 1 step 13 cannot sweep unexpected untracked binaries into the scaffold commit. Cost if wrong: repository size; reversible with a follow-up commit.

## Environment facts (recorded at setup)

- Windows, PowerShell; git 2.45.1 with identity configured (Alex Kovaceski).
- Python 3.13.13 and Node 22.22.2 present; git bash present at C:\WINDOWS\system32\bash.exe.
- Rust toolchain absent; MSVC Build Tools 18 present at C:\Program Files (x86)\Microsoft Visual Studio\18\BuildTools (linker available to cargo).
- Network: static.rust-lang.org, crates.io, index.crates.io and gnu.org reachable; win.rustup.rs blocked.

## Progress

Task 1: dispatched

Task 1: implementation complete (06606f9) — DONE_WITH_CONCERNS; task review dispatched.
Task 1: minor (deferred): README.md carries an em-dash mojibake (three-byte UTF-8 read as Latin-1); Task 11 rewrites the README and must fix it there.
Ruling: harness shells do not inherit the PATH change rustup made, so every later task must either prepend $env:USERPROFILE\.cargo\bin to $env:Path in each shell invocation or call cargo by full path. Cost if wrong: none (build failures would be confusing, not damaging).
Ruling (2026-09-17, direct human request): the MCP server is now also an agent contract. Slice 1 Task 8 additionally publishes docs/agents/mcp-tools.json and docs/agents/mcp-agent.md, adds the okf.diff tool and exposes two MCP resources; the shipped MCP agent itself lands in Slice 5 (design section 4.4, roadmap Slice 5 workstreams 9 to 12) so that AI-generated MBSE is designed for from the start rather than retrofitted. Cost if wrong: roughly two extra documents and one extra tool in Slice 1.

Ruling: Task 2 was dispatched while the Task 1 review was still in flight. Task 1 implementation is finished (its implementer is idle), its reviewer is read-only on a fixed diff, and Task 2 touches only docs/okf/ - disjoint from every Task 1 file - so there is no concurrent implementer and no file contention. Cost if wrong: Task 2 commits may interleave with a hypothetical Task 1 fix commit; both are additive and reconcile trivially.
Task 2: dispatched

Task 1: COMPLETE. Review verdicts: spec compliance PASS, task quality PASS, no Critical or Important findings. Closed at 06606f9.
Task 1: minor (resolved): README em dash mojibake. Root cause was the controller: document content written through PowerShell here-strings was mangled as CP1252, and the brief inherited it. Repaired centrally in b1c9c5d (README, roadmap, plan, both designs: 202 replacements plus arrows and box-drawing glyphs).
Ruling: avoid PowerShell here-strings for document content containing non-ASCII; write documents with the file tools instead, and run the mojibake survey after document-heavy steps. Cost if wrong: cosmetic defects only.
Ruling: Task 2 owns the commit for docs/okf; controller commits must never stage files an implementer is actively writing. Cost if wrong: a half-written file could be committed under the wrong message.

Task 2: implementation committed (183bac4).
Ruling: the Task 2 implementer stalled in its verification step for over 15 minutes after both files were written; the controller interrupted it, verified the deliverables directly (schema parses, required fields correct, 9 definitions, 9 spec sections, clean encoding), and instructed it to commit and report. Cost if wrong: none - the controller verification is recorded here and the task review still runs independently.

Task 3: implementation complete (4bcb0f9) - DONE; review dispatched. Controller verification: 5 files present (fixture 64282 B, mdzip 216922 B, standalone 141712 B, two READMEs), fixture numbers confirmed 99/165/25/49/9/8, collateral untouched, copies byte-identical (size and sha256 checked in the review package).
Ruling: rustfmt and clippy are not present (the toolchain was installed with the minimal profile), so Step 13 of the engine tasks would fail. Every engine task must run `rustup component add rustfmt clippy` before its format and lint step; the Task 4 dispatch carries this instruction. Cost if wrong: a lint step fails and is retried, no code damage.
Task 4: dispatched (okf crate plus test-support, workspace root Cargo.toml).

Task 2: review verdicts - spec compliance PASS, task quality FAIL (1 Critical, 5 Important, 3 Minor), every finding inherited from the normative brief rather than from transcription.
Task 2: fix round 1 dispatched to the original implementer. Rulings: (a) keep the schema required array and correct the spec's exportedAt row to 'no (expected)', with a clarifying sentence in section 9; (b) express validation rules 2, 5 and 6 in the schema as minLength 1 for project and reqId, and minItems 1 for graph.nodes; (c) add kind enums to graphNode and graphEdge only, matching section 3, since the validator constrains those and not element.kind; (d) do NOT add a closed enum to graphEdge.label, because the reference corpus carries a dependency edge labelled 'Dependency' and a closed enum would reject the fixture the validator accepts - instead the spec now states that the traceability spellings are checked by the coverage analysis, not by document validation. Cost if wrong: the schema and validator could drift by one rule; the review of the fix round checks alignment.

Task 3: review verdicts - spec compliance PASS, task quality PASS, no Critical findings. One Important finding (HTML fixture not stored byte-identically because autocrlf normalises CRLF to LF in the blob; the working tree only looks right because autocrlf re-expands on checkout).
Ruling: mark the demo fixture binary in .gitattributes with a path-specific rule (sample/demo/*.html binary) rather than a blanket *.html rule, because a blanket rule would mark future portal HTML sources binary and destroy their diffs. Fix round 1 dispatched to the Task 3 implementer, including the required verification (git ls-files --eol must report i/-text and the blob size must be 141712). The plan's Task 1 Step 5 was corrected so a re-run produces the same file. Cost if wrong: the fixture contract would silently differ on Linux CI.

Task 4: BLOCKED then resumed. The implementer found a genuine disagreement between the plan's types and the reference fixture (Activity.partitions is an array of objects with name and represents, not Vec<String>), verified it was the only mismatch across every field, and stopped rather than improvising - correct behaviour.
Ruling: the fixture wins. The plan now defines a Partition struct (name, plus an optional represents) and Activity.partitions is Vec<Partition>; the Task 4 brief was re-extracted with the corrected types. Also approved: the implementer's addition of Default to Summary's derive, which #[serde(default)] on OkfRoot.summary requires. Task 4 resumed as fix round 1.
Task 2: fix round 1 landed (80b4d6e: schema now expresses project and reqId non-empty, graph nodes at least one, node and edge kind enums; spec corrected on exportedAt and on where traceability spellings are checked).
Task 2: fix round 2 dispatched - the OKF spec section 4 and the schema's activity definition still described partitions as strings; both now describe the object shape found in the fixture (name plus optional represents), with a partition definition, and the fix carries a required check that the corpus fixture satisfies it.
Task 3: fix round 1 (302517d) added the gitattributes rule but did NOT rewrite the stored blob. Controller verification failed the acceptance check: git ls-files --eol still reported i/lf and the blob was still 140735 bytes instead of 141712, because git add skipped the file with no stat change. Fix round 2 dispatched with git add --renormalize and two hard acceptance checks (blob size 141712, index side i/-text); a fix round is not accepted on the implementer's word when a concrete check exists.

Ruling: the Task 3 fix round 2 achieved the acceptance check (blob is now 141712 bytes and the index side reports -text), but the implementer did it with git commit --amend on the controller's commit, which folded a binary fixture change into a docs commit and changed its hash. The branch is local and unpushed, so the tree is unharmed; the controller amended the message once so the record describes its content, and the reflog retains both hashes. Ruling recorded: implementers must never amend or rewrite an existing commit - fix rounds commit on top - and every fix dispatch must state this explicitly, because the Task 3 dispatch omitted it (controller error). Future acceptance checks that involve history should also assert the commit graph, not only the file state.

Incident: parallel fix rounds collided in git. The Task 3 fix round 2 used git commit --amend while the Task 2 fix round 2 had staged docs/okf changes, so both sets of changes were folded into one commit (now 243ec85, message describing only the partition ruling and the fixture blob). Content is correct and verified; the commit message under-describes it; the reflog retains earlier hashes.
Ruling: from now on, implementers and fix rounds stage only the paths they own (for example git add docs/okf) and never amend or rewrite a commit. The controller also serialises committing more strictly: a fix round that needs to commit is not dispatched while another agent has staged changes in the index. Cost if wrong: history stays local and unpushed until the branch is finished, so a message-level inaccuracy is recoverable; a content collision would not be.

Task 4: Partition ruling applied by the implementer (the fixture now parses; 9 of 11 tests pass). The controller then ran the suite and diagnosed the remaining 2 failures as a plan defect: diff keyed its attribute map by element id alone, but OKF mirrors every element as both a section item and a graph node, so a graph entry masked removals and renames. Ruling: the diff universe becomes section-scoped (structure:, interfaces:, signals:, requirements:, state:, graphnode:), missing/extra/changed derived from those keys, with element_ids left as the id universe for gate reporting. Fix round 2 dispatched with the corrected code inline. Cost if wrong: the gate would treat a lossy migration as equal - the exact failure modelwrite exists to prevent, which is why this is fixed rather than deferred as minor.

Task 4: two further plan defects found by running the suite and investigating the fixture.
Defect A: OkfRoot.state_machine lacked a serde rename, so the fixture's stateMachine key never bound and validation wrongly reported the section missing. Fixed in the plan and the brief.
Defect B (a real finding about the reference corpus): exactly 2 of the 165 edges reference a source id that appears nowhere else - two dependency edges labelled Satisfy whose element the legacy exporter never emitted as a node. Ruling: unresolved edge endpoints are validation WARNINGS, not errors, so the platform can hold and gate a real exporter's output while reporting the defect loudly; the test now asserts exactly 2 such warnings, pinning the finding in code. The corpus README should record it. Cost if wrong: a document with a dangling reference is not rejected outright - acceptable because the gate's round-trip comparison and the graph health metrics still surface it, and the alternative is refusing our own proof fixture.

Task 4: the implementer applied rulings A and B and correctly reported the knock-on inconsistency: the plan's rejects_dangling_edge_endpoint test still asserted error behaviour while ruling B made unresolved endpoints warnings. Ruling C: the test is renamed warns_dangling_edge_endpoint and now asserts the document stays valid and the warning is present - same protective purpose, new severity. The plan and the brief were corrected; this was a controller omission, not an implementer error.
Observation: cargo test stops at the first failing test binary, so a count reported from a run that did not use --no-fail-fast can understate results. The controller's earlier claim that validation was 7/7 was wrong for that reason; the implementer caught it. Verification counts from now on come from --no-fail-fast or per-binary runs.

Task 4: COMPLETE (588a78d). 11/11 tests pass (7 validation, 4 diff); fmt and clippy clean. Three plan defects were found and corrected during it: the Activity partition shape, the missing stateMachine serde rename, and a diff that keyed elements by id alone so the graph mirror masked removals and renames. Task 4 review dispatched; Task 5 (graph crate) dispatched.
Discovery worth keeping: the reference export contains two Satisfy edges whose source element was never emitted as a node. Recorded in the corpus README, pinned by a test, and reported as a validation warning rather than refused, because the platform must hold a real exporter's output and still report the defect.

Task 4: review verdicts - spec compliance PASS, task quality FAIL. Two Important findings, both plan defects: the section-scoped diff ignored the activities section and every document-level field, so models differing only in activities compared equal; and the edge key's pipe-joined string was non-injective, so a label containing a pipe could alias two edges. Rulings: extend the diff universe to document-level fields (doc:project, doc:okf, doc:exportedAt, doc:summary, doc:provenance, doc:stateMachine) and to each activity (activity:<index>, covering its name, partitions, nodes and edges); make edge keys JSON arrays; add two tests so the gap cannot reopen; expected count 13. Minors deferred with reasons (the broken fixture arrives in Task 6, hash order-sensitivity is documented, the global id set is intended). Fix round 3 dispatched.

Task 4: fix round 3 was NOT applied by the implementer - it went idle without acting on the instruction (a no-op round). Ruling: with the change fully specified and the round burned, the controller applied the ruled fix directly (3b49775) rather than spending another agent round; the independent re-review is the gate and it runs on the fix diff, so authorship is not what is being trusted. Result: 13/13 tests (7 validation, 6 diff), fmt clean, clippy clean.
Task 5: implementation committed (b102653) but the implementer did not write its report; the controller requested it, since the reviewer needs it alongside the diff.
Process note: two agents have now gone idle without completing their final instructed step (Task 2 stalled in verification; Task 4 ignored a fix instruction; Task 5 skipped its report). Mitigation for the remaining tasks: the dispatch states the report and commit as the last two numbered actions with an explicit 'if you stop before this, the task is incomplete' line, and the controller verifies the artefacts rather than the claim.

Task 5: report delivered with two concerns, both resolved or approved: (a) a clippy-required deviation (the Allocate arm of requirement_coverage collapsed into a match guard because clippy 1.98 flags the nested if under -D warnings) - behaviour identical, approved; (b) the implementer correctly refused to stage Cargo.lock because the dispatch said to stage only its own paths - that lockfile change was picked up by the controller's fix commit 3b49775, and the tree is now clean. Task 5 review dispatched.
Ruling: dispatch instructions that tell an implementer to stage only its own paths must also say what to do about workspace-level files such as Cargo.lock. From Task 6 onward the rule is: stage your own paths plus Cargo.lock when your change alters it.
Task 6: dispatched (the gate - the milestone task).

Task 4: re-review of the diff fix returned PASS - every field of OkfRoot is now inside the diff universe (document-level, all element sections, activities, graph nodes, and injective JSON edge keys), 13 tests, no new defect. Three Minors were raised: a stale doc comment on edge_keys, no test for the state-machine capture path, and a note that the reviewed commit range also carried the unrelated graph crate (an artefact of commit ordering, not a defect).
Ruling: rather than defer both Minors to the final review, the controller fixed the two that could mislead a future reader or leave a distinct code path unproven: the comment now describes the JSON-array key, and a new test state_machine_change_is_a_difference mutates the state machine name and asserts doc:stateMachine is reported as changed. Suite is now 14 tests (7 validation, 7 diff), fmt and clippy clean. The third Minor is recorded only. Cost if wrong: none; both changes are additive.

Task 6: COMPLETE (38022ae) - the milestone. Controller verification, run independently: the gate PASSES on the corpus (exit 0; roundtrip equal; 0 isolated; 1 component) and FAILS on the corrupted fixture (exit 1; 1 missing element, 3 missing edges, 1 isolated node, 3 components). Both evidence records are committed and deterministic.
Two real model-health findings now on record about the reference model: (1) coverage is 15 of 25 requirements, so 10 requirements carry no satisfy, refine, verify or allocate link at all - the collateral's own review suspected thin traceability and this measures it; (2) the two dangling satisfy links already recorded. Both are reported by the gate's evidence, not hidden.
Task 6 review dispatched; Task 7 (C ABI) dispatched.

Task 5: review verdicts - spec compliance PASS, task quality PASS, with one Important finding: the Allocate arm of requirement_coverage inserted BOTH endpoints into the covered set, so an allocate edge connecting a function to a part would inflate the covered count reported in the gate evidence. Dormant on the reference corpus (all three Allocate edges are node to node, so allocated is 0) but wrong in general, and inherited verbatim from the plan.
Ruling: fix it. The controller applied the change (f349a3e) rather than spending an agent round: each endpoint is now inserted only when it is a requirement. The same commit adds corpus_coverage_is_pinned, which pins the measured coverage of the reference model (25 requirements, 20 satisfy, 3 refine, 1 verify, 0 allocate, 15 covered, 10 uncovered) so the number the gate reports cannot drift unnoticed. mw-graph is now 5 tests, fmt and clippy clean. A scoped re-review is dispatched; the remaining two Minors (Cargo.lock, now committed; TINY-only coverage testing, now supplemented) are closed by the same change.

Task 5: re-review of the coverage fix returned PASS (covered can no longer be inflated: every arm guards its insert, so covered is a subset of the requirement ids). One Minor remained and was worth closing: the pinned corpus test does not exercise the requirement-to-non-requirement Allocate path that the bug was about, because the corpus has no such edge. Ruling: add the missing regression test rather than leave the fixed path unproven - allocate_edge_counts_only_requirement_endpoints builds a model with an allocate edge from a part to a requirement and asserts covered == 1 (the old code would have reported 2). mw-graph is now 6 tests, fmt and clippy clean (f70afdc).

Task 6: review verdicts - spec compliance PASS, task quality FAIL. One Important finding: the gate panicked (exit 101) on a candidate whose graph section is absent, because it called the graph helpers unconditionally and they require a graph. A lossy candidate rejected by crash is neither a pass nor a failure, and it violates the gate's 0/1/2 exit contract.
Ruling: fix it, and treat an absent graph as a reported failure rather than a crash. The controller applied the change (9a07af0): the graph-dependent checks run only when the candidate has a graph, an absent graph pushes 'integration: candidate has no graph section', and the evidence keeps its schema with empty values so consumers never see a missing object. The same commit makes a value-taking flag without a value a usage error (exit 2) instead of silently ignoring it, and adds candidate_without_a_graph_fails_instead_of_panicking.
Verified end to end by the controller: a graph-less candidate now prints GATE FAIL and exits 1 with the loss, the validation error and the integration note; the corpus self-comparison still prints GATE PASS and exits 0. mw-gate is 4 tests, fmt and clippy clean. A scoped re-review is dispatched. The third Minor (the fixture generator leaves an orphan node and stale summary counts) is accepted as-is: it is deterministic, the gate still fails for the right reasons, and it exercises the warning path.

Task 7: review verdicts - spec compliance PASS, task quality PASS, with one Important finding: the C ABI entry points contained expect() calls, which are latent panic-across-FFI sites (unwinding out of extern "C" is undefined behaviour). Unreachable with today's payloads, but the idiom is fragile.
Ruling: remove every panic path rather than accept an unreachable-but-undefined one. The controller applied the change: a single into_c_string helper hands payloads to the caller and returns null instead of panicking, err_string routes through it, the version string uses it, and both serde_json serializations now match on the error instead of expecting. engine/capi now has zero expect() sites, 4 tests pass, fmt and clippy clean. The plan was synced from the live file. A scoped re-review is dispatched. Task 7's two remaining Minors (the unsafe extern marking and the collapsed error strings) are accepted as contract-neutral and recorded.
Process note: the reviewers for Tasks 6 and 7 could not read those implementers' report files (they were absent when the review was dispatched), so they reviewed the diff and the controller reproduction instead. Harmless here, but the practice is now: confirm the report file exists before dispatching a task review.

Task 7: re-review of the capi fix returned PASS with no Critical or Important findings: zero panic paths remain, ownership is correct on every path, and a null return is safe for free_string. One Minor was raised - free_string and the error strings were unaffected, but the reviewers noted --evidence --json still consumes --json as the evidence value; that is pre-existing and out of scope.
Task 6: re-review of the gate fix returned PASS: no candidate input can make the gate panic, the evidence schema is stable, and the roundtrip comparison and exit-code contract cannot be bypassed. One Minor was raised and fixed (e887e40): for a candidate that loses its graph but keeps requirements, coverage reported total 0, understating the model; it now reports the retained requirement count with every retained requirement listed as uncovered, and the graph-less test asserts that.
Task 8: implementation complete (a6afa79); review dispatched. Task 9 (CI) dispatched.

Task 8: implementation complete (a6afa79), 7 tests. The implementer reported one deviation: the brief's diff test expects camelCase keys while okf::diff::DiffReport serialized snake_case, so it mapped keys inside the MCP branch.
Ruling: the inconsistency is at the source, not in the adapter. Every other published contract in the platform is camelCase (the OKF document itself with graphNodes and reqId, the gate evidence with missingElements and componentCount), so DiffReport now carries serde(rename_all = "camelCase") and the MCP branch serializes the report directly, with no hand-rolled mapping to drift. A new test pins the key style so a future rename cannot silently change the contract. Workspace: 36 tests pass, fmt and clippy clean.
Task 9: CI workflow committed (c3a6750).

Task 8: review verdicts - spec compliance PASS, task quality PASS. One Important finding and two Minors.
Important (already resolved): the okf.diff branch hand-rolled a camelCase key mapping. The review said the implementer's report was factually false because DiffReport already carries serde(rename_all = camelCase). Correction recorded: that attribute was added by the controller in f880a42, AFTER Task 8 was committed, so the implementer's report was accurate when written - the reviewer read a working tree that had moved on. The finding's substance was already fixed: f880a42 removed the redundant mapping and made the report serialize directly.
Minor 1 (fixed, 903ae01): the published manifest listed the tools in a different order from tools/list; the manifest now matches the server order exactly.
Minor 2 (fixed, 903ae01): three protocol edges were untested - a notification must get no response, malformed input must return -32700, and a tool failure must appear in the result with isError true rather than as a transport error. Three tests were added; mw-mcp is now 10 tests.
Minor 3 (recorded, no action): mw://evidence/latest is advertised in resources/list and in the manifest but its read path returns empty contents. Faithful to the brief; a binding agent gets an empty result rather than an error. Deferred to the agent work in Slice 5.

Task 10: COMPLETE (1a21f5c). Controller verified: judge unit tests 2 OK, judge on the corpus prints PASS and exits 0, judge on the corrupted fixture prints FAIL with the full reasons and exits 1.
Task 11: COMPLETE (d6af28a). Controller verified the whole sequence on this machine: cargo fmt --check exit 0; cargo clippy --workspace --all-targets -D warnings exit 0; cargo test --workspace --no-fail-fast 39 tests pass (capi 4, gate 4, graph 6, mcp 10, okf 8 + 7); judge unit tests 2 OK; mw-gate corpus self-comparison GATE PASS exit 0; corrupted comparison GATE FAIL exit 1; judge PASS/FAIL exit 0/1.
Slice 1 status: all 11 tasks implemented, every task reviewed individually (Task 1 PASS/PASS; 2 spec PASS with quality fixes; 3 PASS/PASS; 4 PASS after two fix rounds and a PASS re-review; 5 PASS plus a PASS re-review; 6 spec PASS with an Important fix and a PASS re-review; 7 PASS/PASS plus a PASS re-review; 8 PASS/PASS). Final whole-branch review dispatched at d6af28a.

Task 11: COMPLETE (d6af28a). The implementer reported four concerns, all sound: a concurrent commit from this session landed during its work (the MCP protocol tests), the brief's expected mcp count was therefore stale (10, not 7), the brief's README block carried CP1252 mojibake which it normalised rather than reproducing, and the branch has 36 commits rather than the 11 the brief anticipated because tasks produced multi-commit work. None affects the deliverable.
Encoding: a final sweep found 40 further double-encoded punctuation sequences that the earlier repair missed (README, the OKF spec, the plan and the evidence index). Normalised and committed; a follow-up check reports zero suspects across the docs.

Final whole-branch review: verdict MERGEABLE, no Critical findings, three Important findings - all fixed in one pass, as the process prescribes.
Important 1: the MCP graph.stats tool called graph_stats unguarded, so a graph-less OKF crashed the mw-mcp process; the gate had been hardened for exactly this case but the tool had not. Now it returns a tool error, and the tool has its first two tests (a healthy corpus and a graph-less document).
Important 2: GraphStats and CoverageReport serialised snake_case while every other published contract is camelCase. Both now carry serde(rename_all = camelCase), with a test pinning the key style, so the Slice 5 agent binds to one consistent JSON.
Important 3: the determinism claim was prose only. Added evidence_is_identical_across_runs (two runs plus an independently parsed copy), and the controller proved it in practice: both committed evidence records regenerate byte-for-byte identical to the committed files. CI now regenerates them and diffs, so a future timestamp or nondeterministic ordering fails the build.
Workspace is now 43 tests (capi 4, gate 5, graph 7, mcp 12, okf 8 + 7), fmt and clippy clean.
Deferred Minors from the final review, recorded for triage in later slices: element_ids is dead public API; the C ABI gate cannot request strict coverage; modelwrite_version leaks by design; nothing syncs docs/agents/mcp-tools.json with the server tool list; activities are keyed by index so reordering them reads as a change; the JSON schema requires summary while the validator only warns; CI never exercises the declared MSRV 1.75; the MCP spec resource resolves only from a source checkout; canonical_hash is order-sensitive; the validator's single id set gives cross-section duplicates a misleading message; the corrupted fixture keeps an orphan node and stale summary counts; mw://evidence/latest reads empty.
Two design observations carried forward to Slice 4: (1) the diff compares doc:exportedAt, doc:summary and doc:provenance, so a semantically lossless re-export that refreshes a timestamp or recomputes summary counts will FAIL the gate - the migration workflow needs a policy for derived fields before the connectors land; (2) CoverageReport.covered is a deduplicated union across the four link kinds, not their sum (24 edges against covered 15), and the evidence should document that relationship.
