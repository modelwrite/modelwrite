[package]
name = "mw-analytics"
version = "0.2.0"
edition.workspace = true
rust-version.workspace = true
license.workspace = true
description = "Cross-model analytics and data fusion whose findings are traceable to sources with weakest-link confidence."
repository = "https://github.com/modelwrite/modelwrite"
readme = "README.md"
keywords = ["modelwrite","analytics","data-fusion","confidence"]

[lib]
name = "analytics"

[dependencies]
mw-okf = { path = "../okf", version = "0.2.0" }
mw-graph = { path = "../graph", version = "0.2.0" }
serde.workspace = true
serde_json.workspace = true
sha2.workspace = true
hex.workspace = true

[dev-dependencies]
mw-test-support = { path = "../test-support", version = "0.2.0" }
serde_json.workspace = true
