# mw-binding

The binding contract: a versioned adapter between a source standard and OKF plus a round-trip harness. Part of the [modelwrite](https://github.com/modelwrite/modelwrite) MBSE engine (AGPL-3.0-or-later).
