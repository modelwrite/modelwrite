# mw-binding-xmi

The SysML v1 XMI binding that reads the stated subset into OKF and writes it back. Part of the [modelwrite](https://github.com/modelwrite/modelwrite) MBSE engine (AGPL-3.0-or-later).
